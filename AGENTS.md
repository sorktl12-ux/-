# Agent instructions (모든 에이전트)

Before any non-trivial task in this workspace:

1. Read `shared/README.md`, `shared/PROFILE.md`, `shared/MEMORY.md`.
2. Reuse existing tools under `google-calendar-sync/` and `iphone-data-organizer/` instead of reinventing.
3. After completing user-visible work, append a dated note to `shared/MEMORY.md` (no secrets).
4. Calendar events: current year only unless user says otherwise; use `[학사]` / `[목]` prefixes.
5. This Cloud Agent does not automatically share memory with other sidebar projects (e.g. english-call) unless they use the same Environment or include `shared/`.
